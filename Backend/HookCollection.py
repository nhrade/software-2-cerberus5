import os
import sys

import hook;

class HookCollection:
	def __init__(self, name, sequenceNumber, status, description, associationCounter):
		self.name = name
        self.sequenceNumber = sequenceNumber
		self.status = status
		self.description = description
		self.associationCounter = self.associationCounter
	
	def updateCollectionStatus(self):
		if self.status = true
			self.status = false
		else:
			self.status = true
	
	def editCollectionSequenceI(self, number):
		self.sequenceNumber = number
		
	def updateHookStatus:
	
	def provideHookCollectionName:
	
	def searchForHook:
	
	def executeHooks: